{
  "name": "src",
  "version": "1.0.0",
  "description": "Tech: Node.js, Express, MongoDB, Mongoose, JWT\r ## Quick start\r 1. clone repo\r 2. cp .env.example .env and fill MONGO_URI / JWT_SECRET\r 3. npm install\r 4. npm run seed   # optional demo data\r 5. npm run dev    # dev mode",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node src/server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "dotenv": "^17.2.2",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.18.0"
  }
}
